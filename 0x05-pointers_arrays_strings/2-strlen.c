#include "main.h"
#include <string.h>

/**
*_strlen - func that returns len of str
*@l: len of string
*@s: parameter of func
*
*a function that returns the len of str
*/
int _strlen(char *s)
{
	int l;

	l = strlen(s);

	return (l);
}
