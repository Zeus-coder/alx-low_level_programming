#include "main.h"
#include <string.h>

/**
*_strlen - func that returns len of str
*@s: parameter of func
*
*Return: len of str
*
*a function that returns the len of str
*/
int _strlen(char *s)
{
	int l;

	l = strlen(s);

	return (l);
}
